{
  "name": "dayplain-push-server",
  "version": "1.0.0",
  "description": "Push notification backend for Dayplain habit tracker",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "generate-vapid": "node -e \"const wp = require('web-push'); const keys = wp.generateVAPIDKeys(); console.log('VAPID_PUBLIC_KEY=' + keys.publicKey); console.log('VAPID_PRIVATE_KEY=' + keys.privateKey);\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "node-cron": "^3.0.3",
    "web-push": "^3.6.7"
  }
}
